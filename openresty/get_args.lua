-- local cjson = require "cjson"

-- test = {}

-- test['name'] = 'lestat'
-- test['age'] = '18'

-- local data = ngx.req.get_body_data()

-- ngx.header['Api-Version'] = 'v1'
-- ngx.header['Api-Powered-By'] = 'zmartec'
-- ngx.header['Server'] = cjson.encode(test)

--res = ngx.location.capture('/test',{ method = ngx.HTTP_POST, body = 'hello, world' })
